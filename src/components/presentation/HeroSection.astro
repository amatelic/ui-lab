<div class="w-screen h-screen px-3 flex justify-center">
    <div
        class="w-full h-full grid grid-cols-4 py-8 gap-6 max-w-[1200px] mx-auto px-4"
    >
        <!-- Hero Title: More impactful and benefit-driven -->
        <div class="col-start-1 col-end-3 row-start-1 row-end-2">
            <h1
                class="text-8xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-black to-gray-500"
            >
                UI Labs
            </h1>
        </div>

        <!-- Personalized Greeting: Friendly but professional -->
        <div class="col-start-2 col-end-4 row-start-2 row-end-3">
            <h3 class="text-3xl font-bold text-gray-800 dark:text-gray-200">
                Hey there! 👋
            </h3>
            <p class="text-lg text-gray-600 dark:text-gray-400 mt-2">
                I’m Anže, a frontend dev experimenting with UI components.
            </p>
        </div>

        <!-- Value Proposition: Clear, concise, and action-oriented -->
        <div class="col-start-3 col-end-5 row-start-3 row-end-4">
            <h2 class="text-4xl font-bold text-gray-900 dark:text-gray-100">
                Explore interactive UI experiments below hope you enjoy them!
            </h2>
        </div>
    </div>
</div>

<style>
    .ascii-output {
        --ascii-font-size: 7.488888888888889px;
        --ascii-line-height: 7.488888888888889px;
        --ascii-margin-x: 48.00000000000006px;
        --ascii-margin-y: 0px;
        padding: 0;
        margin: var(--ascii-margin-y, 0) var(--ascii-margin-x, 0);
        font-family:
            Courier New,
            monospace;
        font-size: var(--ascii-font-size, 12px);
        line-height: var(--ascii-line-height, 12px);
        display: block;
        text-align: center;
        overflow: hidden;
        box-sizing: border-box;
        white-space: pre;
        transform: none;
        position: relative;
    }
    .ascii-output span {
        display: inline-block;
    }
</style>
