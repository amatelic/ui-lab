---
import { MapContainer } from "../components/presentation/MapContainer";
import { AudioExample } from "../components/presentation/AudioExample";
import Layout from "../layouts/Layout.astro";
import { ErrorBoundary } from "../components/chat/components/ErrorHandling";
import { MotionClock } from "../components/chat/components/clock/MotionClock";
---

<Layout>
    <div class="flex flex-col justify-center w-full p-8">
        <ErrorBoundary>
            <div class="w-full h-full p-8">
                <MotionClock client:only="react" />
            </div>
            <!-- <div class="w-full h-full">
                <MapContainer client:only="react" />
            </div> -->
            <!-- <AudioExample client:only="react" /> -->
        </ErrorBoundary>
        <!-- <MapContainer client:only="react" /> -->
        <!-- <div class="w-screen h-screen">
            <Test client:only="react" />
        </div> -->
    </div>
    <!-- <MediaContainer client:only="react" />

    <div class="flex justify-center w-full">
        <!-- <HeroSection /> -->
    <!-- <ChatSwitch
        client:only="react"
        message={{
            id: "23312123",
            content: "./paza-moduless.mp3",
            files: [],
            avatar: "bla",
            role: "assistant",
            type: "audio",
            timestamp: new Date(),
        }}
    /> -->
</Layout>
